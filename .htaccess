# URLs antigas (.html) continuam funcionando e são redirecionadas para .php
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^([a-z0-9-]+)\.html$ $1.php [R=301,L]
</IfModule>

# Não exibe listagem de diretórios
Options -Indexes

# Página inicial padrão
DirectoryIndex index.php index.html
